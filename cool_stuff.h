#ifndef cool_h
#define cool_h

enum {
	LIT=1, OPR, LOD, STO, CAL, INC, JMP, JPC, SIO
};

enum {
	RET=0, NEG, ADD, SUB, MUL, DIV, ODD, MOD, EQL, NEQ, LSS, LEQ, GTR, GEQ,
};

enum {
	OUT=0, INP, HLT
};

#endif
